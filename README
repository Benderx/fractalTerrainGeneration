It creates the fractal image on the right when you run the program, and then createsa a large 2D representation of a heightmap on the left side that could be used as terrain in a 2D environment.

Made by Andrew Duerig.
